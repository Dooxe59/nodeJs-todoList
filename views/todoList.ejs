<!DOCTYPE html>
<html>

<head>
  <title>Node js todo list</title>
</head>

<body>
  <h1>Node js todo list</h1>
  <ul>
    <% if (!todolist.length) {%>
    <span>Rien dans la todo list :)</span>
    <% } %>
    <% todolist.forEach((item, index) => { %>
    <li>
      <form action="/todo/delete/<%= index %>">
        <input type="submit" value="Supprimer" /> - <%= item %>
      </form>
    </li>
    <% }); %>
  </ul>
  <form action="/todo/add/" method="post">
    <label for="newItem">Item à ajouter: </label>
    <input type="text" name="newItem" id="newItem" required />
    <input type="submit" value="Ajouter" />
  </form>
</body>

</html>